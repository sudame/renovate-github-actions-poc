name: demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:node-24.18.0-chrome-149.0.7827.200-1-ff-152.0.3-edge-149.0.4022.98-1
    steps:
      - run: echo "Hello, world!"
