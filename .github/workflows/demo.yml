name: demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:node-24.16.0-chrome-148.0.7778.178-1-ff-151.0.2-edge-148.0.3967.83-1
    steps:
      - run: echo "Hello, world!"
